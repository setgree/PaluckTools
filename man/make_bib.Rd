% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_bib.R
\name{make_bib}
\alias{make_bib}
\title{Generate BibTeX Bibliography from DOIs}
\usage{
make_bib(
  data,
  doi_column = "doi",
  bib_file = "./refs.bib",
  filter_dois = TRUE,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing a column with DOI information}

\item{doi_column}{Name of the column containing DOIs (default: "doi")}

\item{bib_file}{Path to the output .bib file (default: "./refs.bib")}

\item{filter_dois}{Logical, whether to filter for valid DOI format (starts with "10.").
If TRUE, only entries matching DOI pattern will be processed. If FALSE, all
non-NA values in doi_column will be attempted. (default: TRUE)}

\item{overwrite}{Logical, whether to overwrite existing .bib file. If FALSE,
new entries will be appended (default: FALSE)}

\item{verbose}{Logical, whether to print progress messages (default: TRUE)}
}
\value{
Invisibly returns a character vector of successfully processed DOIs.
Also creates/updates the specified .bib file.
}
\description{
Creates a BibTeX (.bib) file from a data frame containing DOI information.
This function fetches bibliographic information from DOIs and writes them
to a .bib file suitable for use with LaTeX/BibTeX.
}
\details{
This function uses \code{RefManageR::GetBibEntryWithDOI()} to fetch bibliographic
information from DOI.org. It requires an internet connection and makes API
calls, so processing large numbers of DOIs may take time.

The function will:
\itemize{
\item Filter for unique DOIs (and optionally for valid DOI format)
\item Skip NA values
\item Attempt to fetch each DOI's bibliographic information
\item Write results to a .bib file
\item Print warnings for DOIs that couldn't be processed
}
}
\examples{
\dontrun{
# Using the built-in MAP reduction dataset
library(dplyr)
data(map_reduction_data)

# Generate bibliography from DOIs
make_bib(
  data = map_reduction_data,
  doi_column = "doi",
  bib_file = "my_references.bib"
)

# You can also provide just a vector of DOIs
dois <- c("10.1016/j.appet.2025.108233", "10.1017/bpp.2018.25")
doi_df <- data.frame(doi = dois)
make_bib(doi_df, bib_file = "test.bib")
}
}
\seealso{
\code{\link{write_dockerfile}} for reproducibility
}
\concept{writing functions}
